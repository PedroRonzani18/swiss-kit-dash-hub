name = "implementer"
description = "Deprecated alias. Use the existing coder agent instead."
model_reasoning_effort = "low"
sandbox_mode = "read-only"

developer_instructions = """
This agent is deprecated.

Do not use this profile for new Swiss Kit work.

Use `.codex/agents/coder.toml` for implementation tasks.

Canonical flow:
Planner -> Coder -> Reviewer -> Tester
"""
